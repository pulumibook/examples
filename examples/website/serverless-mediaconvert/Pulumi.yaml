name: serverless-mediaconvert
runtime: nodejs
description: A TypeScript program that deploys a serverless video-encoding pipeline on AWS.
template:
  description: A TypeScript program that deploys a serverless video-encoding pipeline on AWS.
  config:
    aws-native:region:
      description: The AWS region to deploy into
      default: us-west-2
